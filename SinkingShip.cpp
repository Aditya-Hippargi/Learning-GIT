#include <iostream>
#include <string>
#include <vector>
void solve(){
    int n;std::string name,job;

    std::cin>>n;
    std::vector<std::string> vec(n);
    for(int i=0;i<n;i++){
        std::cin>>name>>job;
        if(job=="captain") vec[n-1]=name;
        else{
            if(job==)
        }
    }
}

int main(){

}